<Window x:Class="BankingSystem.Desktop.Views.LoginWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="PagnanBank - Acesso"
        Height="580" Width="420"
        WindowStartupLocation="CenterScreen"
        ResizeMode="NoResize"
        Background="{StaticResource BackgroundBrush}">
    <Grid Margin="28">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto" />
            <RowDefinition Height="*" />
        </Grid.RowDefinitions>

        <StackPanel Grid.Row="0" Margin="0,10,0,20" HorizontalAlignment="Center">
            <Border Width="64" Height="64" CornerRadius="16" Background="{StaticResource PrimaryBrush}">
                <TextBlock Text="P" FontSize="34" FontWeight="Bold" Foreground="White"
                           HorizontalAlignment="Center" VerticalAlignment="Center" />
            </Border>
            <TextBlock Text="PagnanBank" Style="{StaticResource H1}"
                       HorizontalAlignment="Center" Margin="0,12,0,0" />
            <TextBlock Text="Acesse com suas credenciais" Style="{StaticResource Muted}"
                       HorizontalAlignment="Center" />
        </StackPanel>

        <Border Grid.Row="1" Style="{StaticResource Card}" VerticalAlignment="Top">
            <StackPanel>
                <TextBlock Text="E-MAIL" Style="{StaticResource FieldLabel}" />
                <TextBox Text="{Binding Email, UpdateSourceTrigger=PropertyChanged}" />

                <TextBlock Text="SENHA" Style="{StaticResource FieldLabel}" />
                <PasswordBox x:Name="PasswordBox" PasswordChanged="PasswordBox_OnPasswordChanged" />

                <TextBlock Text="{Binding ErrorMessage}"
                           Foreground="{StaticResource DangerBrush}"
                           TextWrapping="Wrap" Margin="0,12,0,0"
                           Visibility="{Binding HasError, Converter={StaticResource BoolToVis}}" />

                <Button Content="Entrar" Style="{StaticResource PrimaryButton}"
                        Command="{Binding LoginCommand}" Margin="0,20,0,0"
                        IsDefault="True" />

                <Grid Margin="0,14,0,0">
                    <TextBlock Text="Ainda não tem conta?" Style="{StaticResource Muted}" VerticalAlignment="Center" />
                    <Button Content="Criar conta" Style="{StaticResource NavButton}"
                            HorizontalAlignment="Right" Width="130" Height="34"
                            HorizontalContentAlignment="Center" Foreground="{StaticResource PrimaryBrush}"
                            Command="{Binding RegisterCommand}" />
                </Grid>

                <Border Background="#EEF3F8" CornerRadius="8" Padding="12" Margin="0,20,0,0">
                    <StackPanel>
                        <TextBlock Text="Credenciais de demonstração" Style="{StaticResource H2}" FontSize="13" />
                        <TextBlock Text="Admin: admin@bank.local / Admin@123" Style="{StaticResource Muted}" Margin="0,4,0,0" />
                        <TextBlock Text="Cliente: cliente@bank.local / Cliente@123" Style="{StaticResource Muted}" />
                    </StackPanel>
                </Border>
            </StackPanel>
        </Border>
    </Grid>
</Window>
